"""cloud_agent_gateway adapters — single-file bridges to upstream nanobot APIs.

Every upstream dependency is isolated in one adapter module.
When upstream nanobot changes its internal API, only the adapter needs updating.
"""