"""Cloud Agent Gateway — framework-agnostic cloud deployment layer.

Sits in front of any AI agent framework, handling OAuth identity,
WebSocket pass-through with identity injection, and cloud platform
auto-detection (HF Spaces, ModelScope, etc.).
"""

__version__ = "0.1.6"
